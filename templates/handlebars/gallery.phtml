{% verbatim %}
<script type="text/x-handlebars" data-template-name="gallery">
    <section id='gallery'>
        <p>In the galleries: {{ title }} </p>
        {{#each photos}}
            <p>{{ title }}</p>
        {{/each}}
	</section>
</script>
{% endverbatim %}