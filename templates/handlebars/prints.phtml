{% verbatim %}
<script type="text/x-handlebars" data-template-name="prints">
    <section id='printsGallery'>
        <div class='products'>
            {{#each}}
                <div class='productThumb'>
                    {{#link-to 'order' id}}
                        <img {{bind-attr src=thumbFile }} />
                    {{/link-to}}
                </div>
            {{/each}}
        </div>
    </section>
</script>
{% endverbatim %}